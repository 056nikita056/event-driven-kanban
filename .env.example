# Database (Neon.tech / Supabase / local)
DATABASE_URL="postgresql://postgres:password@localhost:5432/kanban?schema=public"

# Redis (local or Railway)
REDIS_URL="redis://localhost:6379"

# Socket.io public URL (for client-side connection)
NEXT_PUBLIC_SOCKET_URL="http://localhost:3000"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# AI parser (optional — Claude API)
ANTHROPIC_API_KEY=""

# Node env
NODE_ENV="development"
